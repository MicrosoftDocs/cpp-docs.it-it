---
description: 'Altre informazioni su: C6059'
title: C6059
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6059
helpviewer_keywords:
- C6059
ms.assetid: 343a4cd1-048a-4edf-bb4b-187097bb6093
ms.openlocfilehash: b216fe5728e27ff3fd2208dc05222c8a66f49152
ms.sourcegitcommit: d6af41e42699628c3e2e6063ec7b03931a49a098
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 12/11/2020
ms.locfileid: "97255821"
---
# <a name="c6059"></a>C6059

> avviso C6059: parametro di lunghezza non corretto nella chiamata a \<function> . Passare il numero di caratteri rimanenti, non la dimensione del buffer di \<variable>

Questo avviso indica che una chiamata a una funzione di concatenazione di stringhe sta probabilmente passando un valore non corretto per il numero di caratteri da concatenare. Questo difetto può causare un sovraccarico o un arresto anomalo del buffer. Una cause comune di questo difetto è il passaggio della dimensione del buffer, anziché il numero rimanente di caratteri nel buffer, alla funzione di manipolazione delle stringhe.

## <a name="example"></a>Esempio

Il codice seguente genera questo avviso:

```cpp
#include <string.h>
#define MAX 25

void f( )
{
  char szTarget[MAX];
  char *szState ="Washington";
  char *szCity="Redmond, ";

  strncpy(szTarget,szCity, MAX);
  szTarget[MAX -1] = '\0';
  strncat(szTarget, szState, MAX); //wrong size
  // code ...
}
```

Per correggere il problema, usare il numero corretto di caratteri da concatenare, come illustrato nel codice seguente:

```cpp
#include <string.h>
#define MAX 25

void f( )
{
  char szTarget[MAX];
  char *szState ="Washington";
  char *szCity="Redmond, ";

  strncpy(szTarget,szCity, MAX);
  szTarget[MAX -1] = '\0';
  strncat(szTarget, szState, MAX - strlen(szTarget)); // correct size
  // code ...
}
```

Per correggere questo avviso utilizzando la funzione Safe String Manipulation, vedere il codice seguente:

```cpp
#include <string.h>

void f( )
{
  char *szState ="Washington";
  char *szCity="Redmond, ";

  size_t nTargetSize = strlen(szState) + strlen(szCity) + 1;
  char *szTarget= new char[nTargetSize];

  strncpy_s(szTarget, nTargetSize, szCity,strlen(szCity));
  strncat_s(szTarget, nTargetSize, szState,
                    nTargetSize - strlen(szTarget));
  // code ...
  delete [] szTarget;
}
```

## <a name="see-also"></a>Vedi anche

- [strncpy_s, _strncpy_s_l, wcsncpy_s, _wcsncpy_s_l, _mbsncpy_s, _mbsncpy_s_l](../c-runtime-library/reference/strncpy-s-strncpy-s-l-wcsncpy-s-wcsncpy-s-l-mbsncpy-s-mbsncpy-s-l.md)
- [strncat_s, _strncat_s_l, wcsncat_s, _wcsncat_s_l, _mbsncat_s, _mbsncat_s_l](../c-runtime-library/reference/strncat-s-strncat-s-l-wcsncat-s-wcsncat-s-l-mbsncat-s-mbsncat-s-l.md)
